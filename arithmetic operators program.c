// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,add,sub,mul,div,mod;
    printf("enter no");
    scanf("%d%d",&a,&b);
    add=a+b;
    printf("add = %d",add);
    sub=a-b;
    printf(" sub=%d",sub);
   mul=a*b;
    printf(" sub=%d",sub);
    div=a/b;
   printf(" div=%d",div);
    mod=a%b;
    printf(" mod=%d",mod);
}